services:
  db:
    image: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_TCP_PORT: ${MYSQL_TCP_PORT}
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: [
        "CMD",
        "mysqladmin",
        "ping",
        "-h", "localhost",
        "-P", "${MYSQL_TCP_PORT}",
        "-u", "${MYSQL_USER}",
        "-p${MYSQL_PASSWORD}"
      ]
      interval: 5s
      timeout: 5s
      retries: 5
    ports:  
      - "3306:3306" #for development

  app:
    # build: .
    image: node:22-alpine #for development
    working_dir: /app #for development
    command: npx tsx watch src/server.ts #for development
    volumes: 
      - .:/app # for development
      - ./uploads:/app/uploads # for development
      # - uploads:/app/uploads
    ports:
      - "${PORT}:3000"
    
    environment:
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_TCP_PORT: ${MYSQL_TCP_PORT}
      MYSQL_HOST: db # for development
      # MYSQL_HOST: ${MYSQL_HOST} # for production
      JWT_SECRET: ${JWT_SECRET}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET}
      PORT: ${PORT}
      DATABASE_URL: ${DATABASE_URL}
      UPLOAD_DIR: ${UPLOAD_DIR}
    depends_on:
      db:
        condition: service_healthy

volumes:
  db_data:
  uploads:
    